# HANDOFF.md

> 最終更新: 2026-08-20（全体コードレビューのA・B評価を実装・検証）
>
> このファイルは現在地と残課題だけを置くライブ状態の正。恒久的な設計・規律は `CLAUDE.md`、完了履歴は git log を参照する。

## 現在地

- `main` で全体コードレビュー21件を設計・履歴と再照合し、A評価12件とB評価1件を実装済み。再レビュープランは89/100、ユーザー判断が必要な未決事項はなかったためグリルは省略した。
- 主な改修:
  - 北洋銀行は今年・前年XMLを両方必須とし、片方失敗と全記事0件を明示失敗にした。
  - JA帯広かわにしは全記事の日付DOMが消えた場合を構造変化として検知する。一部欠損とURL日付補完は従来どおり許容する。
  - 汎用抽出の重複判定を記事名だけから `(記事名, URL)` へ変更し、同名の別記事を残す。
  - 外部取得へ共通の接続・読取タイムアウトを設定し、Actionsの収集ステップだけを15分上限にした。失敗時のArtifact保存は維持する。
  - 公開ニュース画面で収集失敗と正常0件を区別し、検索・除外切替中も失敗警告を残す。
  - 金利履歴の表示メタデータ、入力の `manual` 型、不正入力テストを厳密化した。未確認値同士を「変更なし」として畳まない。
  - pre-commitは作業ツリーではなくGitのステージ内容を構文検査する。`send_resend.py` は引数をちょうど2つだけ受け付ける。
- 検証:
  - 指定のPython構文検査に合格。
  - 指定の標準 `unittest` 78件と `git diff --check` に合格。
  - 実サイト8機関をメール送信・成果物保存なしで収集し、全機関 `status=ok` を確認。JA帯広かわにしは取得5・通過2・除外2。
  - 汎用6機関を同一HTMLで旧・新の重複判定相当に比較。追加で残る同名別URLは、年度の異なる金利改定PDFなど実在する別記事だった。
  - このセッションでは操作可能なブラウザーが接続されておらず、公開画面の実操作だけ未実施。表示関数と検索・除外切替の契約は固定テストとNode挙動テストで確認済み。

## 今回は変更しない項目（条件付き）

- 記事URLの機関別許可ホスト: 正規外部リンクを含むため、リンク先保証を要件化する時に実リンク監査と一緒に行う。
- 金利画面の探索索引化: 調査日が数百件になるか、実測で遅延が出た時に行う。
- Markdown表セルとbackfill parser: Markdownを再び機械入力にする場合だけ一体で変更する。
- Artifact・JSONの増加対策: `output/` が数百本規模になった時、年次アーカイブと一体で設計する。
- Claude抽出経路: 凍結を継続し、再有効化時に入力境界・構造化出力・再検証・切断・timeout/retry・例外分類・依存分離を一括対応する。

## 残課題（優先度順）

1. **8/24週次実行の本番確認**: JA帯広かわにしが純粋な記事名でメールと公開サイトへ初反映されること、公開ジョブのpushリトライを含め正常終了することを確認する。
2. **収集失敗表示の実操作確認**: 失敗を含む固定JSONで、検索入力・除外切替後も警告が残ることをブラウザー接続可能なセッションで1回確認する。
3. **金利表の横スクロールと左3列固定**: 列が自然にはみ出した時に実操作確認する。現状は列数不足で横スクロールが発生しない。
4. **将来の年次アーカイブ**: `output/` が数百本規模になったら `output/archive/YYYY/` を導入し、日付探索とArtifact転送も同時に見直す。

## 環境メモ

- 本番は GitHub Actions の `.github/workflows/weekly-news-report.yml`。週次は月曜05:00 JST、手動実行は `workflow_dispatch`。
- 秘密情報は `.env`（Git管理外）とGitHub Secretsに置く。workflowへ渡すのは `RESEND_API_KEY` と `REPORT_TO` だけ。
- リポジトリはpublic。個人情報をコミットしない。コミットメールは設定済みのGitHub noreplyを維持する。
- pre-commitをスキップしない。`core.hooksPath=.githooks` を使用する。
- push先は `origin/main` のみ。参照専用 `C:\Users\mano\src\AI-trend-weather-News` は変更禁止。

## 次回の再開手順

1. `git status --short --branch` と本ファイルを確認する。
2. 直近Actionsを確認する場合は、8/24週次実行の収集・Artifact・publish結果を順に見る。
3. コード変更時は次を通す。

```bash
python -m py_compile scripts/collect_and_send.py scripts/update_rate_history.py scripts/emailer.py send_report.py send_resend.py
python -m unittest discover -s tests -v
```
