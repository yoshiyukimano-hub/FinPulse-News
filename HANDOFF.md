# HANDOFF.md

> 最終更新: 2026-08-31（マイカーローンの初回データが入り、抽出値・レポート・メール・画面をすべて確認。残課題は3件）
>
> このファイルは現在地と残課題だけを置くライブ状態の正。恒久的な設計・規律は `CLAUDE.md`、完了履歴は git log を参照する。

## 現在地

### マイカーローンの初回データを確認（2026-08-31・全項目一致）

- 上流の週次が 09:16 JST に success（定時 07:00 に対し約2時間の遅延。GitHubのcronは最大1時間以上ずれることがあるので、月曜朝に無くても即異常とみなさない）。FinPulse へ `dcf2c96` で金利履歴が入った。
- **マイカーローン9機関15行が初めて公開された**。値は2026-08-27〜28に実サイトで確認した期待値と**全15行一致**。特に注意すべきとしていた2点も正しい: 北海道労働金庫は住宅ローン表の「轟ローン 2.350%」ではなく自動車ローン表の 2.850／3.650 を採れている。北洋銀行は通常プラン（3.200／3.800）とゼロカーボン応援プラン（2.800）が別行になっている。
- 画面（公開サイト `#car`）で凡例1行・北陸のみ赤字注記・最安バッジ2件（変動2.450 釧路／固定1.950 十勝）を確認。
- 上流のレポートHTMLとメールにも「マイカーローン金利比較」セクションが出た。変化一覧に15件が「新規確認」として並び、競合状況・支店での提案にも反映されている。成果物は run `33344098715` のArtifact。
- 住宅ローンは48行すべてに 08/31 が入り、先週から金利変動なし。ニュースは8機関すべて `status=ok`（通過32件・除外276件・収集失敗0）。
- **`PRODUCT_NAME_OVERRIDES` が本番の週次上書きに耐えることを確認**した（北洋が「ほくようスペシャル住宅ローンα」のまま）。前日入れた対策の実地検証。
- ルートの `index.html` はmeta refreshだけだったためハッシュを落としており、`/#car` で共有しても住宅ローンタブが開いていた。JSがあるときはハッシュ付きで `location.replace` する1行を足して直した（meta refreshはJS無効時の保険として残す）。

### 監査の出典URLと北洋の商品名を反映（2026-08-29）

- **`url` を商品ページへ**（監査5-2・A案）: `loan-features.json` の北海道銀行2商品と北洋2商品の `url` を、金利一覧ページから商品ページへ変更した。北海道銀行は `loan/jyutaku/kinri-kinds/koteikinri_sentaku.html`／`tesuryo_teiritsu.html`、北洋は両商品とも `person/loan/house/j_jloan.html`（＝「ほくよう住宅ローン（ＮＰ保証口）」の商品ページ。α と α定率型はこの商品の金利タイプで、商品説明書PDFへのリンクもここにある）。4ページとも実機で存在と見出しを確認済み。
- `rate-history.json` 側の `url` は変更しない。上流 `config/banks.json` の `loan_products[].url` は**金利を取りに行く先そのもの**で、`update_rate_history.py` が毎週上書きするため。結果として2つのJSONで同じ商品の `url` が異なるが、条件比較タブの出典としてはこちらが正しい。
- **北洋の商品名の括弧ゆれ**（監査5-3）: 実ページを確認したところ、公式サイトに「定額型」の語は無く、金利一覧・商品ページとも「**ほくようスペシャル住宅ローンα**」だった（本文では「手数料定額型／手数料定率型」と説明）。両JSONを公式表記へ統一し、上流から毎週上書きされる分は `PRODUCT_NAME_OVERRIDES` に `("northpacific", "northpacific_teigaku")` を追加して吸収した。上流の名称は金利抽出の商品名照合に使うため上流は変更していない。
- 「α」だけでは手数料定額型と読み取りにくいが、公式表記を優先した（**ユーザー確定・このままでよい**）。事務手数料の列に定額・定率の内容があるため実害は小さい。括弧付きへ戻す案は却下済みなので、次に読む人は再検討しなくてよい。
- 検証: 119テスト中118合格。失敗1件は `test_pre_commit_hook` の既知の環境依存（stderr が None になる cp932 問題）で、今回の変更とは無関係。

### 機関フィルタと収集失敗表示の整合を実機確認（2026-08-29・問題なし）

- 前回未確認だった「絞り込みでチェックを外したとき、サマリーの『収集失敗 N機関』が追従するか」を実機で確認し、全パターンで表示内容と一致した。修正は不要。
- 確認方法: `docs/` と `output/data/` をリポジトリ外の一時領域へコピーし、2026-08-24 レポートへ失敗2機関（北海道銀行 `fetch_failed`／JA木野 `extract_failed`）を注入してローカル配信した。リポジトリは変更していない。
- 見た挙動: 8/8で失敗2機関 → 北海道銀行を外すと7/8で失敗1機関（節ごと消滅）→ JA木野も外すと失敗表示自体が消える → 失敗2機関だけ残すと2/8で通過0件・失敗2機関。検索「住宅」＋除外も表示ONでも件数と画面が一致した。
- 構造上ずれない理由: `docs/index.html:1002-1010` で選択外の機関を先に `continue` で飛ばしてから `failedCount` を数えており、集計と描画が同じループにある。
- 副次的に確認した仕様: 検索語を入れても失敗機関の赤い警告は消えない（`index.html:1007`）。収集失敗を検索で見落とさせないための意図的な挙動なので、そのままとする。

### 条件比較データの全件監査と反映（2026-08-29）

- 機関ごとに調査を分けて公式サイト・商品概要説明書PDFに当たり、15商品×11項目=165セルを1件ずつ突合した。結果は `docs/loan-features-audit-2026-08-28.md`（反映状況の節つき）。帯広信金と北見信金「きたしん住宅ローン」は全項目一致、要修正は約25セル。
- **見つかった事実誤りのうち重いもの**: 北洋銀行2商品の借入期間と金利引下げ条件が2025年1月版の旧内容だった（現行は新築住宅2〜50年、引下げは「給与振込＋1項目」か「3項目以上」）。北海道銀行2商品は団信の上乗せ割当が逆で、8疾病の31〜51歳は上乗せなし・0.10％は全疾病だった。北洋の「繰上返済0円」は保証会社への5,500円が別途必要。
- **保証料を3値化**（ユーザー確定）: `guarantee_warn`（真偽）を廃止し `guarantee_status` へ。`included` 灰／`separate` 赤／`conditional` 橙。北陸標準型は金利内包を選べば北海道地区限定で上乗せ0％、道労金は会員だけ無料、北見ベスト住いるは公式に記載がなく不明で、いずれも2値では表せなかった。`schema_version` は2。
- **JA帯広かわにし**: 自社サイトには条件がほとんど無いが、JAバンク公式のJAネットローンに同JA専用の商品概要があり、組合員必須・年齢・年収・限度額・期間・団信を採用した。一次情報の範囲を「自ドメイン＋その機関専用の公式ページ」と定めた（`CLAUDE.md`）。
- **機関どうしの優劣は書かない方針に変更**（ユーザー確定）。「今回の10機関で最も安い」等の断定9か所を削除。4件はデータ自身で反証できた（機関数も9で誤り）。テストで文言を禁止した。
- 十勝信組のクレディセゾン提携ローンとフラット35は条件の異なる別商品なので `unique` から削除した。
- 未反映として残したもの: 借地上・連棟式など物件形態の例外、350万円未満かつ2年の少額短期の例外、`url` を金利一覧から商品ページへ変更、北洋の商品名の括弧ゆれ（`rate-history.json` と同時に直す必要あり）。

### 表示の調整（2026-08-29）

- **列幅をドラッグで変えられるようにした**。金利タブは「金融機関・商品」列、条件比較タブは機関列と11項目の各列。見出し右端のハンドルをドラッグ、矢印キーでも調整、ダブルクリック・Home・Escで既定に戻る。幅は列ごとに localStorage へ保存し、種別（住宅／マイカー）で分ける。
- 検索窓を250pxへ拡張（「金融機関・商品名で検索」が末尾まで出なかったため）。「機関を絞り込む」を「金融機関を絞り込む」に改め、3画面ともボタンとポップオーバーを広げた。
- タブ順を 新着ニュース → 住宅ローンの条件比較 → 住宅ローン金利情報 → マイカーローン金利情報 に変更（ユーザー指示）。
- マイカーローンタブは、金利データが0件でも「変動／固定」の種別ボタンを出すようにした。従来はデータが無いとボタンごと消え、種別を選べなかった。

### 住宅ローンの条件比較タブを新設（2026-08-28）

- 目的: 金利表の数字だけでは実質の負担が見えないため、金利以外の条件（保証料・事務手数料・繰上返済・団信・年齢・年収・資格・限度額・期間・引下げ条件・独自の特徴）を10機関15商品ぶん並べて比較できるようにした。
- **調査でいちばん重要な発見**: 住宅ローンの保証料は機関ごとに扱いが割れる。**同じ北陸銀行でも定額型は保証料別・定率型は込み**。ほかに十勝信組とJA帯広かわにしが別途、労金は会員かどうかで変わる。金利だけを並べた比較は誤読を生む。
- 表示は2か所:
  - 住宅ローン金利タブ: 機関名の下に「保証料の扱い」と「事務手数料」の2行（ユーザー選択の案2）。色分けは2026-08-29に3値化した（上記）。
  - 新タブ「住宅ローンの条件比較」: `docs/loan-features.html` を4つ目のiframeタブとして追加。左列固定＋横スクロール、項目グループ（費用／借りられる条件／特徴）の切替、機関の絞り込み、検索。
- データは `docs/data/loan-features.json` が正本。**週次の自動収集には載せない**（人手で更新する編集データ）。金利履歴と `product_id`・機関順・機関名・商品名が一致することをテストで固定しているので、上流で商品が増減したらテストが落ちて気づける。
- 条件JSONが読めなくても金利表は出す（注記だけ消える）作りにした。
- タブが4つになり狭い画面で収まらないため、タブ行を切り取りから横スクロールへ変えた（切り取ると最後のタブに手が届かない）。
- 検証: FinPulse 118テスト合格。ローカルサーバーで4タブとも実機表示を確認（金利タブの2行注記、条件比較タブの列グループ切替、コンソールエラーなし）。

### マイカーローンの保証料表示と2機関の追加（2026-08-28）

- 表示方式: 表の上に「表示金利はすべて保証料込です」の凡例を1行置き、上乗せがある機関だけ機関名の下に赤字で注記する。公開サイトのタブ（`?dataset=car`）と週次レポートHTML・メールの両方に同じ形で入れた。住宅ローンは機関ごとに保証料の扱いが分かれる（JAは保証料別）ため凡例を出さない。
- 経路: 上流 `config/banks.json` の `guarantee_note` → `scraper.py` → `compare.py` の `car_loan_table` → FinPulse `update_rate_history.py` → `car-loan-history.json` の行 → ビューアー。抽出（Gemini）は通さず、設定に書いた事実をそのまま運ぶ。FinPulse側で改行潰し・80文字上限に正規化し、`validate_history` でも型と長さを見る。
- 保証料の調査結果（2026-08-28に全機関の実ページで確認・9機関すべて保証料込）:

| 機関 | 保証料 | 確認した内容 |
|---|---|---|
| 北陸銀行 | 込（**例外あり**） | 金利表示に保証料込。ただしオリコ保証になると年1.60%上乗せ。1次保証に落ちると自動で2次保証（オリコ）へ回る仕組みがある |
| 北海道銀行 | 込 | 金利の中から銀行が保証会社へ支払う方式。借入時の一括払い不要 |
| 北洋銀行 | 込 | 保証料を含んだ金利を表示、と明記 |
| 帯広信用金庫 | 込 | 金利表示に保証料率含む。別途支払い不要 |
| 釧路信用金庫 | 込 | 各金利に「変動金利・保証料込」と併記 |
| 北見信用金庫 | 込 | 商品表の保証料等の欄に「金利に含みます」 |
| 北海道労働金庫 | 込 | 金利表の脚注に、保証料は上記金利に含まれる旨 |
| 十勝信用組合 | 込 | 融資利率に「固定金利・保証料込」と併記 |
| JA帯広かわにし | 込 | 保証機関ごとに「（保証料込）」と併記 |

- 追加した2機関（当初の対象外判断は誤りだった）:
  - 帯広信用金庫「おびしんカーライフプラン」変動2.85（最優遇・保証料率含む）。金利一覧ページには基準金利3.95しか無く、最優遇は商品ページ側にある。一覧ページだけを見て判断したのが誤りの原因。
  - 北見信用金庫「カーライフプラン プライム」を期間別に2行（5年以内2.28／5年超2.78）。優遇後の数値はお知らせページの返済例の表に載っており、一覧ページには通常金利しか無い。金利欄が変動・固定の2つしかないため、期間差は商品名で分けている。プライムでない「得徳くん」（2.48／2.98）は登録しない（ユーザー判断）。
- 北洋銀行はプランごとに2行へ分けた（ユーザー確定）。「通常プラン」変動3.20（借入1,000万円以下・1,001万〜2,000万円は2.80）／固定3.80と、「ゼロカーボン応援プラン」変動2.80（電気自動車・PHEVの新車をWEB契約する場合・固定なし）。1行にまとめると条件の違う金利が混ざり、一般の車を買う人が2.80を自分に適用できると誤読する。
- 検証: FinPulse 102テスト・上流61テスト合格。9機関10商品ぶんのレポートデータを作って `car-loan-history.json` へ取り込み（14行）、レポートHTMLを生成して凡例1件・注記1件（北陸のみ）・最安バッジ（変動2.450／固定1.950）を確認した。確認後、履歴JSONは元の空の状態へ戻した。
- **未確認**: 公開サイト側の見た目はブラウザー拡張が応答しなくなり実機確認できていない。次にブラウザーを触るときに `?dataset=car` を開いて凡例と赤字注記の位置を1回見る。
- 北洋銀行の固定金利は最優遇3.80で確定（ユーザーが実サイトで確認）。以前は代替手段の読み取りが3.80／3.90／4.00と揺れて未確定だった。

### マイカーローン金利タブ（2026-08-27・両リポジトリともpush済み）

- 方針: 収集は上流 `報告自動化ツール`（financial-report-tool）に追加し、FinPulseは取り込みと表示だけを持つ。金利種別は変動・固定の2種。ユーザー確認済み。
- FinPulse側（コミット `fa4209e`）:
  - `scripts/update_rate_history.py` を `Dataset` で住宅ローン／マイカーローンの2系統に一般化。`car_loan_table` を `docs/data/car-loan-history.json` へ積み上げる。既定は `--dataset all`（レポートに含まれる種別をすべて更新）。
  - `rate_contract` は version 1・2 の両方を受け入れる。version 1（住宅ローンのみ）が来た週はマイカーローンを更新しない。
  - `docs/index.html` に3つ目のタブ「マイカーローン金利情報」を追加（ハッシュは `#car`）。`docs/rate-history.html` は `?dataset=car` で読むJSON・タイトル・説明・掲載範囲を切り替える。既知のデータセット名だけを受け付ける。
  - `docs/data/car-loan-history.json` は空（rows 0件）で新規追加。初回の週次実行までは画面に「金利データがありません」が出る。
- 上流側（`C:\Users\mano\src\報告自動化ツール` ・コミット `bff3008`）:
  - `rate_contract.py` を version 2 にし `car_loan_rate_fields` を追加。`scraper.py`・`gemini_extraction.py`・`compare.py`・`check_output.py`・`rate_validation.py` にマイカーローン経路を追加。金利抽出の商品名照合は住宅ローンと共通化した。
  - `config/banks.json` に7機関の `car_loan_products` を追加（実サイトで確認済み・下表）。
  - `weekly_report.yml` の反映ステップで `docs/data/car-loan-history.json` も `git add` する。
- 登録した7機関（すべて引下げ後の数値が公表されているもの）:

| 機関 | 商品 | URL | 確認値（2026-08-27時点） |
|---|---|---|---|
| 北陸銀行 | マイカーローン | `/kinri/loan.html` | 固定 2.200 |
| 北海道銀行 | マイカーローン | `/kinri/new_loan.html` | 変動 3.075／固定 3.650 |
| 北洋銀行 | マイカーローン（通常プラン／ゼロカーボン応援プラン） | `/person/loan/mycar/` | 通常: 変動 3.20・固定 3.80／ゼロカーボン: 変動 2.80 |
| 釧路信用金庫 | しんきん保証基金付カーライフプラン | `/kushiro/car.html` | 変動 2.45 |
| 北海道労働金庫 | 轟ローン | `/site/company/rate-02.html` | 変動 2.850／固定 3.650 |
| 十勝信用組合 | マイカーローン（オリコ提携） | `/customer/loan/mycar_loan.html` | 固定 1.95 |
| JA帯広かわにし | マイカーローン | `/service/bank/rate/` | 変動 2.50／固定 4.00 |

- 帯広信用金庫・北見信用金庫は当初「引下げ後の公表なし」として対象外にしたが、これは調査の誤りだったため2026-08-28に登録した（下記の保証料の節を参照）。
- 検証:
  - FinPulse 98件・上流58件のunittestを実行。FinPulseの `test_pre_commit_hook` 1件だけエラーだが、これは変更前（HEAD）でも同じく失敗するこの環境のcp932デコード問題で、今回の変更とは無関係。
  - 上流の `build_report_data` で作った実際の `report_data_*.json` を FinPulse の `update_rate_history.py` に通し、契約v2 → `car-loan-history.json` → 画面表示まで通ることを確認した（確認後、履歴JSONは元の空の状態へ戻した）。
  - ブラウザーで `docs/index.html` を開き、タブ切替・タイトル・説明文・空状態・データ投入時の表（変動／固定チップ、最安バッジ）を実機確認した。
- push は FinPulse（`f61f5ba`）→ 上流（`bff3008`・`18514da`）の順で完了済み。今後この2リポジトリの契約を変える時も、**FinPulseを先にpushする**（逆順だと、上流だけ新しい version になった週に取り込めない）。
- 次の一手: 上流の週次実行後にマイカーローン9機関の抽出結果を実サイトと突き合わせる（2機関は2026-08-28に追加）。

- 8/24の週次自動実行を本番確認済み。`main` はリモートと同期（最新コミットは `git log -1` を見る）。
  - Actions run `32664037601`「週次金融機関新着情報レポート」は success。後続のPagesビルドも success。自動コミット2本（レポート・金利履歴）まで到達した。
  - 収集は8機関すべて `status=ok`、収集失敗0機関。通過32件・除外287件。
  - JA帯広かわにしは取得4・通過2・除外2。記事名は日付前置きもパンくず混入もない純粋な形で、Markdown・日付別JSON・`by-institution.json` の3か所すべてに同じ値が入った。`index.json` にも 2026-08-24 を追加済み。`af77518` の狙いどおり。
  - レポートメールも 8/24 05:19 JST に届いていた（件名「【金融機関新着情報】2026-08-24（通過 32件）」）。受信側でゴミ箱に入っていたが本人が `その他セミナー/AI/ツール結果` ラベルへ移動済み。送信側の問題ではない。8/31以降も同じラベルへ入るかは要観察。
  - 金利履歴に追加された48件を全機関の実サイトと照合し、全件一致を確認した。48件すべてが8/17と同値だが、各サイトの適用開始日は7/1〜8/10で改定が無かったためで、コピーではなく「その日に確認して同値」である。基準も48件すべて揃っている（`CLAUDE.md` の設計決定事項を参照）。
- JA木野へ専用抽出 `ja_kino` を追加し、記事名から日付とカテゴリの連結を解消した。実サイトで取得10・通過3・除外5を確認。掲載日はDOMの `.date` だけを正とし、URL内の日付（PDF自体の日付で掲載日と1日ずれる）は補完に使わない。
- 除外ルールに照合先の指定 `target`（`title` 既定 / `url`）を追加した。記事名では分けられない区分だけURLで落とすための最小拡張で、`unless` も同じ照合先を見る。`target` を使うのはJAおとふけの1件のみで、他7機関は挙動不変。
- JAおとふけへ除外2件を追加した。`【イベント】`（金利・貯金・ローン・共済・貸付を含む場合は救済）と `/hokurennews/`（target=url）。どちらもホクレン給油所の記事で、`【イベント】ウェルカムキャンペーン`はガソリン給油の販促だった。実サイトで通過2件・給油所系4件の除外を確認。金融店舗の営業時間変更の記事は残る。
- JA木野の `include_keywords` に「プライムレート」を追加した。専用抽出化で`短期プライムレートの引き上げについて`が「ローン」を含まず落ちるようになったため。同種記事が通過・除外に分かれる状態は解消済み。
- 北海道労働金庫の商品名を「住宅ローン（定額型）」「すまいる上手（定率型）」に変更した。`product_name` は上流の `financial-report-tool` から毎週上書きされるため、`update_rate_history.py` の `PRODUCT_NAME_OVERRIDES` で履歴へ書く直前に置き換える方式にした。JSONの直接編集だけでは翌週戻る。
- 全体コードレビュー21件のA評価12件とB評価1件は `ba2a064` までで実装済み（内容は git log を参照）。
- 検証:
  - 指定のPython構文検査に合格。標準 `unittest` は91件すべて合格、`git diff --check` も合格。
  - JA木野は実サイトで収集を1回通し、`status=ok`・取得10・通過3・除外5を確認。
  - JAおとふけも実サイトで1回通し、通過2件と給油所系4件の除外を確認。
  - 公開画面の収集失敗表示を実機で確認済み。2機関（`fetch_failed` / `extract_failed`）を注入した固定JSONをローカル配信し、検索文字列を入れた状態でも、除外表示をONにした状態でも、赤い警告2件とサマリーの「収集失敗 2機関」が両方残ることを確認した。検証データはリポジトリ外の一時領域に置いたため、`output/` は変更していない。

## 今回は変更しない項目（条件付き）

- 記事URLの機関別許可ホスト: 正規外部リンクを含むため、リンク先保証を要件化する時に実リンク監査と一緒に行う。
- 金利画面の探索索引化: 調査日が数百件になるか、実測で遅延が出た時に行う。
- Markdown表セルとbackfill parser: Markdownを再び機械入力にする場合だけ一体で変更する。
- Artifact・JSONの増加対策: `output/` が数百本規模になった時、年次アーカイブと一体で設計する。
- Claude抽出経路: 凍結を継続し、再有効化時に入力境界・構造化出力・再検証・切断・timeout/retry・例外分類・依存分離を一括対応する。
- 条件比較の物件形態の例外（借地上・連棟式の期間短縮）と少額短期の手数料例外: 該当者が限られ行が伸びるため反映しない（ユーザー判断・決着済み）。粒度が帯広信金・北見信金と不揃いになる点は承知のうえ。監査の他の指摘はすべて反映済みで、詳細は `docs/loan-features-audit-2026-08-28.md` の反映状況の節。

## 残課題（優先度順）

1. **条件比較データの鮮度管理**: `docs/data/loan-features.json` は人手更新で、確認日は2026-08-29（全項目を公式一次情報と突合済み）。金利・手数料・団信の改定は反映されないので、半年に一度は各機関の商品概要説明書を読み直す。北洋銀行のように商品リーフレットが差し替わると気づけないため、更新の有無は各機関のリーフレットの日付欄を見る。上流で住宅ローンの商品が増減した週は `tests/test_loan_features.py` が落ちるので、その時にJSONへ商品を足す。
2. **金利表の横スクロールと左3列固定**: 列が自然にはみ出した時に実操作確認する。現状は列数不足で横スクロールが発生しない。マイカーローンは調査日1列だけなので、こちらで先に起きるのは住宅ローン側。
3. **将来の年次アーカイブ**: `output/` が数百本規模になったら `output/archive/YYYY/` を導入し、日付探索とArtifact転送も同時に見直す。

## 環境メモ

- 本番は GitHub Actions の `.github/workflows/weekly-news-report.yml`。週次は月曜05:00 JST、手動実行は `workflow_dispatch`。
- 秘密情報は `.env`（Git管理外）とGitHub Secretsに置く。workflowへ渡すのは `RESEND_API_KEY` と `REPORT_TO` だけ。
- リポジトリはpublic。個人情報をコミットしない。コミットメールは設定済みのGitHub noreplyを維持する。
- pre-commitをスキップしない。`core.hooksPath=.githooks` を使用する。
- push先は `origin/main` のみ。参照専用 `C:\Users\mano\src\AI-trend-weather-News` は変更禁止。

## 次回の再開手順

1. `git status --short --branch` と本ファイルを確認する。
2. 直近Actionsを確認する場合は、9/7以降の週次実行の収集・Artifact・publish結果を順に見る（8/24・8/31分は確認済み）。マイカーローンは2週目から前週比の増減矢印が出るので、9/7は表の見え方が変わる最初の週になる。
4. コード変更時は次を通す（`test_pre_commit_hook` 1件はこの環境のcp932依存で常に落ちる。他が通れば合格とみなす）。

```bash
python -m py_compile scripts/collect_and_send.py scripts/update_rate_history.py scripts/emailer.py send_report.py send_resend.py
python -m unittest discover -s tests -v
```
