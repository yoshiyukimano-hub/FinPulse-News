# HANDOFF.md

> 最終更新: 2026-08-27（8/24週次実行の本番確認と金利48件の実サイト照合が完了）
>
> このファイルは現在地と残課題だけを置くライブ状態の正。恒久的な設計・規律は `CLAUDE.md`、完了履歴は git log を参照する。

## 現在地

- 8/24の週次自動実行を本番確認済み。`main` は `967e77d` でリモートと同期。
  - Actions run `32664037601`「週次金融機関新着情報レポート」は success。後続のPagesビルドも success。自動コミット2本（レポート・金利履歴）まで到達した。
  - 収集は8機関すべて `status=ok`、収集失敗0機関。通過32件・除外287件。
  - JA帯広かわにしは取得4・通過2・除外2。記事名は日付前置きもパンくず混入もない純粋な形で、Markdown・日付別JSON・`by-institution.json` の3か所すべてに同じ値が入った。`index.json` にも 2026-08-24 を追加済み。`af77518` の狙いどおり。
  - 金利履歴に追加された48件を全機関の実サイトと照合し、全件一致を確認した。48件すべてが8/17と同値だが、各サイトの適用開始日は7/1〜8/10で改定が無かったためで、コピーではなく「その日に確認して同値」である。基準も48件すべて揃っている（`CLAUDE.md` の設計決定事項を参照）。
- 過去の週次実行では、全体コードレビュー21件のうちA評価12件とB評価1件を実装済み（`ba2a064` ほか）。再レビュープランは89/100、ユーザー判断が必要な未決事項はなかったためグリルは省略した。
- 主な改修:
  - 北洋銀行は今年・前年XMLを両方必須とし、片方失敗と全記事0件を明示失敗にした。
  - JA帯広かわにしは全記事の日付DOMが消えた場合を構造変化として検知する。一部欠損とURL日付補完は従来どおり許容する。
  - 汎用抽出の重複判定を記事名だけから `(記事名, URL)` へ変更し、同名の別記事を残す。
  - 外部取得へ共通の接続・読取タイムアウトを設定し、Actionsの収集ステップだけを15分上限にした。失敗時のArtifact保存は維持する。
  - 公開ニュース画面で収集失敗と正常0件を区別し、検索・除外切替中も失敗警告を残す。
  - 金利履歴の表示メタデータ、入力の `manual` 型、不正入力テストを厳密化した。未確認値同士を「変更なし」として畳まない。
  - pre-commitは作業ツリーではなくGitのステージ内容を構文検査する。`send_resend.py` は引数をちょうど2つだけ受け付ける。
- 検証:
  - 指定のPython構文検査に合格。
  - 指定の標準 `unittest` 78件と `git diff --check` に合格。
  - 実サイト8機関をメール送信・成果物保存なしで収集し、全機関 `status=ok` を確認。JA帯広かわにしは取得5・通過2・除外2。
  - 汎用6機関を同一HTMLで旧・新の重複判定相当に比較。追加で残る同名別URLは、年度の異なる金利改定PDFなど実在する別記事だった。
  - このセッションでは操作可能なブラウザーが接続されておらず、公開画面の実操作だけ未実施。表示関数と検索・除外切替の契約は固定テストとNode挙動テストで確認済み。

## 今回は変更しない項目（条件付き）

- 記事URLの機関別許可ホスト: 正規外部リンクを含むため、リンク先保証を要件化する時に実リンク監査と一緒に行う。
- 金利画面の探索索引化: 調査日が数百件になるか、実測で遅延が出た時に行う。
- Markdown表セルとbackfill parser: Markdownを再び機械入力にする場合だけ一体で変更する。
- Artifact・JSONの増加対策: `output/` が数百本規模になった時、年次アーカイブと一体で設計する。
- Claude抽出経路: 凍結を継続し、再有効化時に入力境界・構造化出力・再検証・切断・timeout/retry・例外分類・依存分離を一括対応する。

## 残課題（優先度順）

1. **JA木野の専用抽出**: 記事名に日付とパンくずが連結される。実データは `2026.08.10JAバンク/貯める貯金金利の引き上げについて`。汎用抽出がリンク内の全テキストを連結するためで、サイト構造は `[日付][カテゴリ][記事名]` が1リンク内に並ぶ。JA帯広かわにしと同じく `config.json` に `"scraper": "ja_kino"` を足し、`SCRAPERS` へ専用抽出を追加する。`af77518` の実装が手本。
   - 放置した場合の実害は2つ。(a) 通過・除外の判定はタイトル文字列に対して行うため、記事の中身と無関係にカテゴリ名で判定が動く。JA木野の設定は通過に「ローン」「金利」、除外に「共済」「JA共済」を含み、現行パンくず（JAバンク/貯める・借りる）では偶然セーフだが、サイトがカテゴリ名を変えると全件通過や全件除外に転ぶ。(b) 重複判定は(記事名, URL)なので、日付表記が `2026.08.10` から `2026/08/10` に変わるだけで同一記事が別記事として再登場する。
   - 既存レポートのタイトル遡及修正は別判断。過去の `output/` は人間向け保存物なので、そのままでも整合は取れる。
2. **8/24レポートメールの着信確認**: Actions側は success だが、実際に `REPORT_TO` へ届いたかはリポジトリからは分からない。Gmail検索で1回確認する。
3. **収集失敗表示の実操作確認**: 失敗を含む固定JSONで、検索入力・除外切替後も警告が残ることをブラウザー接続可能なセッションで1回確認する。
4. **金利表の横スクロールと左3列固定**: 列が自然にはみ出した時に実操作確認する。現状は列数不足で横スクロールが発生しない。
5. **将来の年次アーカイブ**: `output/` が数百本規模になったら `output/archive/YYYY/` を導入し、日付探索とArtifact転送も同時に見直す。

## 環境メモ

- 本番は GitHub Actions の `.github/workflows/weekly-news-report.yml`。週次は月曜05:00 JST、手動実行は `workflow_dispatch`。
- 秘密情報は `.env`（Git管理外）とGitHub Secretsに置く。workflowへ渡すのは `RESEND_API_KEY` と `REPORT_TO` だけ。
- リポジトリはpublic。個人情報をコミットしない。コミットメールは設定済みのGitHub noreplyを維持する。
- pre-commitをスキップしない。`core.hooksPath=.githooks` を使用する。
- push先は `origin/main` のみ。参照専用 `C:\Users\mano\src\AI-trend-weather-News` は変更禁止。

## 次回の再開手順

1. `git status --short --branch` と本ファイルを確認する。
2. 直近Actionsを確認する場合は、8/31以降の週次実行の収集・Artifact・publish結果を順に見る（8/24分は確認済み）。
3. コード変更時は次を通す。

```bash
python -m py_compile scripts/collect_and_send.py scripts/update_rate_history.py scripts/emailer.py send_report.py send_resend.py
python -m unittest discover -s tests -v
```
