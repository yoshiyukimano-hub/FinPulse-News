# HANDOFF.md — 現在地・残課題の正（SSoT）

このファイルは **Codex と Claude Code の両方が読み書きする、ライブな引き継ぎ状態の唯一の正**。
「いま何がどこまで進んでいて、次に何が残っているか」はここだけに書く（メモリや他ファイルに二重に書かない）。

- **規律・構成は `CLAUDE.md`**。**進め方とプロトコルは `AGENTS.md`**。**収集ロジック本体は `scripts/collect_and_send.py`**。
- 節目（意味のあるコミット）ごとに本ファイルを更新し、**コードと同じコミットに含める**。
- 更新時は下の「最終更新」を必ず書き換える（古さを検知できるように）。

**最終更新**: 2026-08-17（更新者: Claude Code）/ 全体コード再レビュー第2回のプラン作成（未実装・承認待ち、下記参照）。

---

## 現在地

### 【2026-08-17】全体コード再レビュー第2回（24指摘・プラン作成済み・未実装）

- `/full-code-review`（4観点マルチエージェント）の24指摘を `/re-review` で検証し、実装プランを **`docs/re-review-plan-2026-08-17.md`** に作成した（プラン採点 90/100）。**コードは未変更・ユーザー承認待ち**。
- 区分: A評価6件（#1 日付抽出の実在日検証・#2 routine-prompt-template 廃止明記・#5 publish push リトライ・#15 glob 絞り込み・#19 sorted key・#24b 空状態文言）、B評価4件（#4 入口サニタイズ XML/programmatic 限定・#8 知識の一元化・#11 requests 私有属性解消・#24a 絞り込み維持）、C評価9件、D評価5件。詳細・着手順・保護リストはプラン文書が正。
- 最重要は #1: `extract_date_from_text` / `extract_date_from_url` が実在日検証なしで「0155-24-12」等を通し、ヴューアーJSON三点セット生成が丸ごと失敗する（メール・Markdownは fail-open で継続）。
- 前回（2026-08-01）の C/D 決定（Claude 経路凍結・使い捨てスクリプト・iframe 分離・定数化しない）はすべて維持。今回それらに触れる指摘は保留・却下側へ振り分けた。
- 未回答のユーザー確認: (1) #24a 日付切替時の機関絞り込みを維持へ変えるか、(2) #11 を今回やるか requests 更新時に延期か、(3) #2 テンプレは廃止注記で残すか削除か。

### 【2026-08-17】釧路信金REDの 8/10・8/17 欠落を手動補完

- 釧路信金がページ刷新（令和8年8月1日〜）で金利名称を「新規受付金利」に変えたため、報告自動化ツール側で保証基金付住宅ローンRED が 8/10・8/17 の2週とも全項目取得失敗（null）となり、金利履歴の4行（変動・固定3年・5年・10年）だけ2週分の欠落が生じていた。
- 報告自動化ツール側は `banks.json` の note 追加（`98da15f`）で修正済み・実API 2回一致で検証済み。本番反映は 8/24（月）実行から。
- サイト表示の空欄（—）を避けたいというユーザー要望により、検証済みの現行値で `docs/data/rate-history.json` の 8/10・8/17 を手動補完した: 変動3.325／固定3年1.2／固定5年2.2／固定10年2.6（8/10も同値であることはユーザー確認済み）。固定3年・5年・10年は 8/3 比でそれぞれ +0.05 の利上げ。
- 補完はリポジトリの `validate_history()`＋`write_history_atomic()` 経由で実施し、全40テスト合格。8/24 の自動更新は同日重複を冪等に扱うため衝突しない。

### 【2026-08-06】iframe内スクロールは正常と判明（自動操作側の制約だった）

- ユーザーが公開URLのタブ経由で実操作し、**縦スクロールは正常**（表が流れ、日付見出し行と左端の固定列は留まる）と確認。2026-08-05 に自動操作で動かなかったのは、疑似ホイールイベントが外側の文書に向けて送られ iframe 内の別文書まで届かないという道具側の制約で、**サイト側の不具合ではない**。同日にタブ切替直後の画面撮影が2回タイムアウトしたのも同じ原因と見られる。
- 横スクロールは**確認できる状態にまだ無い**。調査日9列が表の枠に収まっており（右に約570pxの余白）、はみ出していないためスクロールする対象が存在しない。左3列固定の実操作確認は列が増えてからとして残課題1へ分離した。

### 【2026-08-05】北洋の取りこぼし2件を解消（include_keywordsに「手数料」追加）

- 8/3レポートの北洋（通過4件・除外115件）を精査。除外の大半は法人個別案件の公表で、落ちて正しいと確認した。**「通過率が低い」はフィルタの不具合ではなく北洋の告知構成そのもの**。
- 個人が負担する費用の変更が2件落ちていたため、`config.json` の北洋 `include_keywords` に「手数料」を追加した（通過中の「コンビニATM利用手数料の優遇条件変更」と同性質）。
  - 追加で通過: 2026-07-21「cloverVisa」ショッピング利用手数料率改定、2026-06-29 振込組戻手数料の申受け。
- 8/3の実データへ `apply_filters` を再適用し、**意図した2件だけが増える**ことを確認（通過4→6件・除外115→113件）。`include_keywords` は機関ごとの設定のため他6機関は無影響。全40テスト合格。
- 候補として検討した「預金」（7件該当）・「取扱」（4件該当）は、いずれも「〜にお預け入れいただいた**法人**のお客さまを公表します」等の法人向け広報が中心のため採用しない。

### 【2026-08-05】自動蓄積の初回本番確認・公開URL確認（完了）

- 2026-08-03（月）の自動実行が両方とも成功し、`origin/main` へ自動コミット2件が入っていた（`6c08697` ニュースレポート、`5249074` 金利履歴更新）。ローカルもfast-forwardで追随済み。
- 金利履歴JSON: 調査日が8件→**9件**（先頭 `2026-08-03`）。**48行すべてに 8/3 の値が入り48/48で全埋まり**。`is_demo: false`、スキーマ2。掲載は9機関15商品のままで、**手動入力3機関（みずほ銀行・JAめむろ・JA木野）は復活していない**。
- ニュース側 8/3 レポートは**7機関すべて `status: ok`**（帯広信金6・道銀1・北洋4・十勝信組4・JAおとふけ2・JAめむろ2・JA木野1／通過20件）。
- 公開URL（`https://yoshiyukimano-hub.github.io/FinPulse-News/docs/index.html`）をブラウザで確認:
  - 新着ニュースタブが 8/3 を最新として表示。日付リスト先頭も 8月3日。週次更新の公開反映が継続していることを確認。
  - 金利履歴タブが9調査日・9機関で表示。「変更なし」列の畳み込み（07/27〜07/20、06/29）、見出し行と左固定列の固定、未確認セルの「—」、前回比↑（7/06→7/13の利上げ）が動作。
  - **8/2 の見た目変更2件を実機で確認**: 現在列の最安は背景＋「最安」バッジ（釧路信金 変動1.375%、十勝信組 固定5年1.150%・固定10年1.250% 等）、過去列の最安は金色・太字・下線のみでラベルなし。前回セッションで未実施だった目視確認はこれで解消。
- **未解決の小さな観察**: 上部タブ経由（iframe）だと表内側のスクロールがブラウザ自動操作のホイールで動かず、`docs/rate-history.html` を直接開いて確認した。拡張のホイールイベントがiframe内スクロール枠へ届かなかった可能性が高く、実ユーザーの手動操作で再現するかは未確認。次回、手で触って確かめる。

### 【2026-08-02】過去の調査日にも最安表示を追加

- 表示中の行を対象に、各調査日×金利種別ごとの最安値を算出。過去列の最安値は金色・太字・下線のフォント表現だけで示し、「最安」ラベルや追加改行は付けない。同率最安はすべて強調する。
- 現在列は従来どおり背景と「最安」ラベルを維持。検索・金利種別・機関の絞り込みを変えるたび、表示中の行で現在・過去とも再計算する。
- 過去最安の専用クラス、ラベルが現在列だけに限定されること、過去値がインライン表示であることを検査するテストを追加。FinPulse全40テストに合格。内蔵ブラウザが利用できなかったため目視確認のみ未実施。

### 【2026-08-02】金利履歴ビューアーの表示領域を拡大

- 「帯広エリア 金融機関の住宅ローン金利を〜」の説明文を「金利履歴ビューアー」の右側へ移し、従来の `0.8rem` を維持した。
- 検索欄を高さ30pxへ縮め、種別チップ・機関絞り込み・操作列の余白も圧縮。表見出し・各セル・最安バッジの上下余白を詰め、同じ画面高で確認できる表の行数を増やした。
- 「各セル」「掲載範囲」「出典」の3行を表と同じスクロール枠の先頭へ移動。最初は表示し、下へスクロールすると画面外へ流れる一方、日付見出し・左固定列・横スクロールは維持する。
- レイアウト構造・30px検索欄・セル密度・日付見出し固定を検査する4テストを追加し、FinPulse全39テストに合格。内蔵ブラウザが利用できなかったため、この変更の目視確認だけは未実施。

### 【2026-08-02】報告自動化ツールとの金利データ契約を明示

- 親ツールの `report_data_*.json` に `rate_contract`（契約版1・住宅ローン金利4項目の順序付き一覧）を追加し、FinPulse側の `validate_report_data()` で完全一致を必須化した。片側だけで項目追加・改名が起きた場合は、誤った履歴へ黙って保存せず更新を停止する。
- 契約情報導入前の保存済みレポートは、過去データ専用の `normalize_legacy_report()` だけが現行契約を補う。通常の週次入力は契約欠落も拒否する。
- FinPulse全35テスト、親ツール全28テスト、両リポジトリをまたぐ契約受入テストに合格。親ツール側の金利キー重複定義も `scripts/rate_contract.py` へ集約した。

### 【2026-08-01】金利履歴の実データ化・週次自動蓄積（実装・検証済み）

- **現在のJSONの実測値（同日その2・その3の反映後）: 調査日8件（2026-06-22〜07-27）・48行・9機関15商品・旧集計行0・手動入力行0。`is_demo: false`、出典は `financial-report-tool`（06-22〜07-27）の1件のみ。** 履歴の日付はすべて `observation_dates` に収まり、6/22・6/23が47/48、他6日は48/48で全埋まり。
- 復元作業の経緯（その1時点）: 週次レポートメールと成功Actions Artifactから19調査日・105行（現在の商品59行＋旧集計46行）まで広げたが、**同日その2で旧集計46行をJSONごと削除した**ため、旧集計だけが持っていた2026-04-12〜06-15の11調査日も消え、現在は上記の8調査日・48行（その3の手動3機関除外で59→48）。
- **旧集計（`is_legacy`）と `legacy_notice` は現在のJSONに存在しない。画面にも出ない。** `backfill_rate_history.py` で 6/22 より前を再投入すると旧集計行が復活するので、意図しない限り実行しないこと。
- **掲載範囲は「公式サイトから毎週自動取得できる機関のみ」。** 手動入力のみずほ銀行・JAめむろ・JA木野は対象外で、`update_rate_history.py` の `update_history` が `manual: true` の商品をスキップするため自動更新でも復活しない。
- `update_rate_history.py` は、各調査日の確認値をすべて保存するスキーマ2へ移行。`observation_dates` を共通時間軸とし、取得できなかった値は保存しないため、据え置きと未確認を区別できる。旧スキーマ1は自動移行し、通常実行では過去日を拒否、明示的なbackfillだけ許可。同日再投入ではファイルSHA256も不変。
- `backfill_rate_history.py` を追加し、複数の過去JSONを日付順に再構築できる（商品IDのない旧レポートは旧集計行へ正規化するため、現在は原則使わない）。
- `rate-history.html` は、左に機関・商品・種別を固定し、右へ `2026/07/27 → 07/20 → …` と共通調査日を並べる横長表。見出し・検索・凡例を固定し、表の枠だけが縦横にスクロールする。各セルはその日に確認できた実金利だけを表示し、前回比の↑↓、未確認の「—」、出典・対象期間を表示する。全商品で金利が動かなかった連続週は「変更なし」列へ畳む（最新列と最古列は必ず残す）。
- `financial-report-tool` の週次Actions末尾に、生成直後の `report_data_*.json` を使って本リポジトリを更新する連携を実装。Artifactの期限には依存しない。
- リポジトリ間の書き込みには、`FinPulse-News` だけへ書き込める専用Deploy Keyを使用。秘密鍵は `financial-report-tool` の `FINPULSE_DEPLOY_KEY` Secretへ登録済み。ローカル一時鍵は削除済み。
- 自動テストは32件すべて成功。Python・JSON・JavaScript・HTTP 200・差分検査、最新JSONの同日再投入不変も確認した。アプリ内ブラウザーは本セッションで利用できず、新しい横長表の目視操作だけ未実施。
- ユーザー承認を受け、`docs/data/rate-history.json`、`docs/rate-history.html`、`scripts/update_rate_history.py`、`scripts/backfill_rate_history.py`、`tests/test_update_rate_history.py`、本ファイルをコミット・pushする。

### 【2026-08-01】全体コード再レビュー（A評価6件・B評価7件を実装済み）

`/full-code-review` の19指摘を `/re-review` でコード・直近25コミット・既存の `docs/re-review-plan.md` と照合した。以前に決めた fail-open（失敗時も生成済み成果物を残す）、Claude経路の温存、使い捨て補助スクリプト、ニュースと金利画面の分離を維持し、ユーザー承認後にA・B評価13件を実装した。

#### A評価（即着手・6件）

- **#2 金利履歴の入力検証と安全な保存（実装済み）**: 実在日、必須ID、金利の型・0〜100%範囲・重複・既存履歴順を検証し、既存最新日より古い通常入力を拒否。同日訂正は維持。一時ファイルを再読込検証後に `os.replace()`。画面側も空文字を0%扱いしない。
- **#6 日付切替の通信競合（実装済み）**: `docs/index.html` に要求番号を追加し、最新要求だけを表示へ反映。機関モードへ移った場合も保留中の日付通信を無効化する。
- **#7 JSON片方の障害時の縮退表示（実装済み）**: `Promise.allSettled()` で `index.json` と `by-institution.json` を個別に読み、成功した日付/機関モードだけでも利用可能にした。
- **#11 手動レポート送信のパス制限（実装済み）**: `YYYY-MM-DD` の実在日検証、余分な引数の拒否、解決後パスが `output/` 配下であることを確認。
- **#13 workflowのmain限定・重複実行防止（実装済み）**: mainガード、`concurrency`（キャンセルせず直列化）、main checkout、`git push origin main` の明示を追加。
- **#18 pre-commitのファイル名安全化（実装済み）**: NUL区切りで空白・日本語名を1ファイルとして扱う構文検査へ変更。

#### B評価（計画的に・7件、実装済み）

- **#1 取得/送信失敗の状態化（実装済み）**: 機関ごとに `ok / empty / fetch_failed / parse_failed / extract_failed` を保持し、件名・Markdown・JSONへ反映。部分失敗でも成果物を作り、最終終了コードだけ失敗にする。
- **#3 Actions依存固定と権限分離（実装済み）**: Python全依存を `requirements.lock` へバージョン・ハッシュ固定し、全ActionをコミットSHA固定。収集は `contents: read`、公開だけ `contents: write` の別ジョブ。未使用の `ANTHROPIC_API_KEY` はworkflowから除去。
- **#4 公開JSON三点セットの一括更新（実装済み）**: 一時領域で日付別・日付一覧・機関別JSONを生成・検証し、`index.json` を最後に置換。成功マーカーがある場合だけ公開ジョブでJSONをstageする。
- **#5 設定・入力JSONの最小スキーマ検証（実装済み）**: config、ニュースJSON、金利入力/履歴の必須値・型・日付・URL・重複・対応版・順序を検証。既存JSONの `schema_version` 省略はv1として受け入れる。
- **#10 Resend応答状態の分離（実装済み）**: 失敗ログをstatus/request ID中心に制限し、応答本文を出さない。HTTP失敗・2xx非JSON・接続失敗・タイムアウトを区別し、自動送信は日付入り冪等キーを使う。
- **#12 外部応答の容量制限（実装済み）**: HTML/XMLを5MB上限のストリームで取得し、Content-TypeとContent-Lengthを検証。`defusedxml` fallback時は強い警告を出す。
- **#19 最小自動テスト（実装済み）**: Python標準 `unittest` と固定データ・モックで25件を追加。設定・既存JSON、フィルター、応答境界、失敗状態、JSON一式、Resend、金利、手動送信パス、workflow固定を検査する。

#### C評価（保留・3件）

- **#8 Claude経路の堅牢化**: 現configでは未使用。過去決定どおり、再有効化時にsystem指示、構造化出力、型/日付/許可URL、`stop_reason`、timeout/retry、例外分類、環境資料を一括対応。部分修正は安全になった誤認を生むため行わない。
- **#9 Markdown表の特殊文字**: 現行JSONはMarkdown再解析を通らず、`backfill_json.py` は使い捨て。今後もMarkdownを機械入力に使う方針へ変わる場合だけ、出力エスケープとparserを一体で設計する。
- **#15 cwd依存**: workflowはリポジトリ直下実行が契約。構造整理を行う将来タスクで全入出力を同じrepository rootへまとめる場合のみ変更。

#### D評価（実施しない・3件）

- **#14 `test_ip_access.py` のconfig連動**: Cursor Automation用の使い捨て診断で、本番監視ではない。必要なら延命せず別テストとして作る。
- **#16 全面dataclass化**: 現状の小規模パイプラインでは回帰リスクが見返りを上回る。#1で必要な最小の結果状態オブジェクトだけ導入する。
- **#17 HTML/送信入口の統合**: ニュースと金利は名前・ID衝突回避のため意図的にiframe分離。手動送信2本も用途別入口として維持し、送信本体は既に共通化済み。

#### ユーザー決定事項

- **決定済み**: 一部機関の取得失敗は、取得済み成果物を保存・公開したうえでActionsを失敗扱いにして通知する。
- **決定済み**: 現在未使用の `ANTHROPIC_API_KEY` はworkflowから外し、Claude経路を再有効化する際に堅牢化と同時に戻す。
- **決定済み**: 同日レポートの自動メールは重複を防ぎ、明示的な手動再送だけを許可する。
- **決定済み**: 恒久的な自動テストはPython標準 `unittest`、固定JSON、モックで導入し、初回はブラウザ自動操作を含めない。

### 【2026-08-01】ヴューアーを2タブ構成へ復旧（新着ニュース＋金利履歴）

- 2026-07-27 の刷新でヴューアーから新着ニュース画面が消えていた（意図は「タブで金利履歴も見られる」であって、ニュースの置き換えではなかった）。これを復旧。
- `docs/index.html` を刷新直前のコミット `c80152a` の内容へ戻した（新着ニュース画面＋上部タブ）。上部タブは「新着ニュース」／「金利履歴」の2つ。
- 金利履歴マトリクスは `docs/rate-history.html` として独立させ、金利タブから iframe で読み込む（初回表示時のみ遅延読み込み）。刷新前も金利タブは iframe 方式だったため構成は同じ。
  - 1ファイルに統合しない理由: 両画面が `state` / `escapeHtml` / `safeUrl` / `#searchInput` / `#filterButton` などを同名で持っており、素直に混ぜると衝突するため。
- 削除済みの `docs/reports/latest.html`（旧デモHTML）は復活させていない。金利タブの中身は現行の金利履歴マトリクス。
- 検証: ローカル HTTP サーバー＋内蔵ブラウザで両タブを目視確認。ニュース画面（7/27レポート・通過20件・7機関・機関/日付ナビ）と、金利履歴の最安ラベル4件・rowspan結合・機関区切りの太罫線・↑↓・iframe内スクロールが意図どおり動作。

### 【2026-07-27】金利履歴マトリクスの初期実装（2026-08-01にスキーマ2・共通日付列へ更新済み）

- **住宅ローン金利の履歴表**を実装した（現在は `docs/rate-history.html`）。
  - 行 = 機関 × 商品 × 金利種別（変動・固定3/5/10年）、列 = 共通の調査日（左=最新・右=過去）。各セルにその日の確認値を表示し、右隣の前回調査比で上昇↑赤/低下↓ティール。
  - 種別ごとに現在列の最安金利へ「最安」ラベル。機関名セルは(機関×商品)単位で rowspan 結合。機関の変わり目は2pxの太罫線。
  - 検索（機関・商品名）・種別チップ・機関絞り込み・左3列固定に対応。外部ライブラリなし。
- **新設ファイル**:
  - `docs/data/rate-history.json` = 金利履歴データの唯一の正。現在はスキーマ2の実データ（8調査日・48行・9機関15商品）。
  - `scripts/update_rate_history.py` = 積み上げ機構。報告自動化ツール（別リポジトリ `financial-report-tool`）の `report_data_*.json` を入力に、調査日ごとの確認値を追加。同週再実行は冪等。標準ライブラリのみ。使い方: `python scripts/update_rate_history.py <report_data_*.jsonのパス>`。
- **削除**: `docs/reports/latest.html`（デモ版の金利調査レポートHTML。金利履歴マトリクスに置き換わったため不要）。
- **過去データの境界**: 現在の最古は2026-06-22。それ以前は商品別でない旧集計しか無く、直接比較できないため2026-08-01に削除した。今後は週次Actionsが共通調査日と確認値を自動追加する。
- **ニュース収集・メール送信の本体（`collect_and_send.py`・GitHub Actions・`output/*.md`・`output/data/*.json`）は無変更で稼働継続**。ニュース画面は 2026-08-01 の復旧で「新着ニュース」タブとして復活済み。
- 検証済み: rowspan結合・最安判定、共通日付列、未確認値の非補完、新規機関追加、同週冪等、手動入力商品の非保存。2026-08-01にローカルHTTPでブラウザ実表示まで確認済み（見出し固定・左列固定・絞り込み時の畳み直し・列幅・手動3機関の消滅と最安バッジ再計算）。

### （以下は刷新前からのニュース収集ツールとしての現在地）

- 帯広エリア近隣7金融機関の新着情報を週次収集してメール送信する自動化ツール。Phase 3（週次自動化）まで稼働中。
- GitHub Actions が毎週月曜 05:00 JST に収集→フィルタ→Resend送信→`output/YYYY-MM-DD.md` を自動コミット。直近（06-22〜07-20）も7機関すべて取得成功で正常稼働。
- 相対日付補完（`infer_date_from_relative_text`）を本番検証済み: `output/2026-06-22.md` で十勝信組「今月のローン金利」が 2026-06-01＋補完注記付きで正しく通過。補完注記が付いたのはこの1件のみで他機関への誤補完なし。コード変更不要。
- 除外一覧の3ヶ月フィルタを追加（2026-07-19）: `format_report` で、配信日（today）から3ヶ月以上経過した除外項目は列挙しない（日付不明は fail-open で残す）。件数表示と「合計 除外」も表示ベースに修正。月末クランプ付き `date_n_months_ago()` を新設。通過セクションのロジックは不変。
- 静的ヴューアー第1弾を実装:
  - `collect_and_send.py` に日付別JSON・日付一覧・機関別集約JSONの追加出力を実装。JSON失敗時も従来のメール送信は継続する。
  - `scripts/backfill_json.py` で既存Markdownレポート13件を `output/data/` へ変換。初期の旧形式レポートにも対応。
  - `docs/index.html` に日付モード・機関モード・検索・機関絞り込み・除外表示・スマホ表示を実装（外部ライブラリなし）。
- 機関別集約に24ヶ月の保持窓を追加: `build_institution_index` が直近24ヶ月だけを集約し、日付別JSON・index.json・ヴューアーの形式は不変。
- public公開前の準備を実装・コミット済み: 送信先を `REPORT_TO` Secretへ移し、リポジトリ直下にPages用リダイレクト(`index.html`)と`.nojekyll`を追加。
- **git履歴を書き換えてGmailを完全除去（2026-07-19・git-filter-repo）**: (a) 全履歴のファイル内容の個人Gmail実値を `redacted@example.com` に置換、(b) コミット作者/コミッターのGmailを noreply(`270032650+yoshiyukimano-hub@users.noreply.github.com`) に置換。**全コミットのSHAが変わったため、過去のHANDOFF記載やコミットメッセージ中の旧ハッシュ（例 `ad3133b` 等）は無効**。旧履歴を持つstaleブランチ `claude/bold-planck-rxVHX` はリモートから削除済み。旧履歴のバックアップは `C:\Users\mano\src\FinPulse-News-backup-pre-rewrite.bundle`。
- **リポジトリを public 化済み（2026-07-19）**。`REPORT_TO` Secret 登録済み。現行ファイル・全リモート履歴ともに個人Gmailの実値は0件。
- ヴューアー左サイドバーを微修正: 機関リストを日付リストより上に移し、日付リストだけを高密度化。JavaScript・ID・データ形式は不変。
- 再レビュー実装プランのA・B評価項目を実装・コミット済み: 送信失敗時も生成済みレポートをコミット、送信処理の共通化とHTMLエスケープ、`star_keywords` の設定化、除外日付判定の共通化、不要なworkflow環境変数の削除。
- **GitHub Pages の公開確認が完了（2026-07-20）**: Source は `Deploy from a branch`、Branch は `main`、Folder は `/(root)`。公開URLは HTTPS で稼働し、7/20 の最新レポート（通過18件・7機関）がヴューアーに表示されることを確認済み。
- ヴューアーの表示密度を調整（2026-07-20）: 左メニューの金融機関名を日付と同じ文字サイズに統一し、ページ見出しと記事日付を小型化。対象期間・通過件数はタイトル右側の1行表示へ変更し、金融機関見出しの「収集: プログラム」を削除。フォントサイズは維持したまま、上部操作欄・見出し・表・スマホカード・ナビの縦余白と行間をさらに縮小。
- 金融機関の収集・JSON・表示順を `config.json` へ一本化（2026-07-20）: `order_institutions()` を生成処理とバックフィルで共用し、既存13レポートと機関別JSONを設定順へ移行。ヴューアーの固有名による暫定補正は削除済み。未知の機関は削除せず元の順で末尾に残す。
- 機関別画面の「収録レポート」を「収録期間」へ変更（2026-07-20）: 複数の日付タグを、最古日～最新日の1つのタグに集約。PC・スマホとも折り返さない1行表示。
- A・B評価の実装一式（Python、HTML、workflow、pre-commit、依存ロック、`tests/`、本ファイル）は実装・検証済み。コミット `d26bf36`（`コードレビューA・B評価の改善を実装`）として `origin/main` へpush済み。
- 今回の検証: Python構文、標準 `unittest` 25件、既存設定・既存ニュースJSON、フィルター、金利の不正入力と原子的保存、手動送信パス、Resend各応答、JSON三点セットと失敗時保全、依存ハッシュ、Actions SHA・権限、workflow YAML、JavaScript、pre-commit、ローカルHTTPを確認。さらに実サイト7機関を読み取り、全機関 `status=ok` を確認。内蔵ブラウザ未接続のため今回の目視操作だけ未実施。

## 残課題（優先度順）

1. **横スクロールと左3列固定の実操作確認**（優先度低・列が増えてから）: 上部タブ経由の金利履歴で、表を横に動かしたときに左3列（機関・商品・種別）が固定されたままかを実操作で確かめる。**現時点では調査日9列が表の枠に収まっており（右に約570pxの余白）、横方向にはみ出していないため確認できない**。1列あたり約74pxなので、単純計算であと7〜8列＝2か月弱で自然にはみ出す（金利が動かない週は「変更なし」列へ畳まれるため実際はさらに先）。急ぐ場合は `Ctrl` + `+` でページを150〜200%に拡大すれば、はみ出した状態を作って確認できる。
2. **将来の年次アーカイブ**: `output/` が数百本規模になったら、古い年のレポートを `output/archive/YYYY/` に移す。将来は `list_report_dates` とindex.json生成をarchive配下も走査するよう拡張する（現時点では未実装）。

### 完了済み
- ~~iframe内の縦スクロール確認~~: 2026-08-06 ユーザーの実操作で**縦スクロールは正常**と確認。2026-08-05 にブラウザ自動操作で動かなかったのは道具側の制約（疑似ホイールイベントが外側の文書へ送られ、iframe内の別文書まで届かない）で、**サイト側の不具合ではない**。レイアウトにも矛盾なし（外側 `#rateFrame { height: calc(100vh - 44px) }` / 内側 `rate-history.html` の `height:100vh; overflow:hidden` ＋ 表の枠だけ `overflow:auto`。内側の `100vh` は iframe の高さを指すため一致する）。横方向は列不足で未確認のため残課題1へ分離。
- ~~北洋の通過率が低い~~: 2026-08-05 調査のうえ対応完了。**フィルタの不具合ではなく、北洋の告知が法人向けに偏っているのが実態**だった。8/3レポートの除外115件を精査したところ、大半は「◯◯株式会社様と『サステナブル経営支援ローン』の契約を締結しました」型の法人個別案件の公表・私募債・企業版ふるさと納税で、落ちて正しい。個人が負担する費用に関わる取りこぼしが2件あったため、北洋の `include_keywords` に「手数料」を1語追加した（通過4→6件・除外115→113件）。候補の「預金」「取扱」は法人向け広報が中心のため見送り。
- ~~自動蓄積の初回本番確認~~: 2026-08-05 完了。8/3 の自動実行で調査日9件・48/48全埋まり、手動入力3機関の非復活、ニュース7機関 `status=ok` を確認。
- ~~公開URLでの再確認~~: 2026-08-05 完了。2タブ・9調査日・9機関・見出し/左列固定・最安表示（現在列バッジ／過去列の金色下線）をブラウザで確認。
- ~~ヴューアー自動公開の継続確認~~: 2026-08-05 完了。8/3 レポートが公開画面へ反映され、週次更新が継続していることを確認。
- ~~A・B評価の本番確認~~: 2026-08-05 完了。上記の 8/3 Actions成功と公開URL確認をもって本番動作を確認。
- ~~金利履歴の実データ化と週次自動蓄積~~: 2026-08-01 ローカル実装・検証完了。過去復元でいったん19調査日・105行まで広げたが、同日中に旧集計と手動入力3機関を除外し、8調査日（2026-06-22〜07-27）・48行・9機関15商品まで整理。**2026-08-03の自動実行で9調査日へ増加**。
- ~~金利履歴ビューアーの表示改善10件・手動入力3機関の除外~~: 2026-08-01 実装・ブラウザ実表示確認完了（コミット `b17d54b` `f970c62`）。テスト34件OK。
- ~~再レビューB評価7件~~: 2026-08-01 実装完了。標準unittest 25件、依存・workflow検査、実サイト7機関の読み取り確認を通過。
- ~~再レビューA評価6件~~: 2026-08-01 実装・機械検証完了。内蔵ブラウザ未接続のため画面の目視操作だけ未実施。
- ~~相対日付補完の本番確認~~: 2026-07-19 検証完了。`output/2026-06-22.md` で意図どおり動作、誤補完なし。
- ~~コードレビュー指摘の改修~~: 2026-07-19 の再レビュー実装プランのA・B評価項目を実装。C評価項目は意図的設計・使い捨てのため変更しない。
- ~~GitHub Pages 有効化と表示確認~~: 2026-07-20 確認完了。`main` の `/(root)` から公開され、最新レポートもヴューアーへ反映済み。
- ~~金融機関順序の一本化~~: 2026-07-20 完了。`config.json` を唯一の順序元とし、既存JSONの移行とヴューアー暫定補正の削除まで実施。

## 環境メモ

- 秘密情報は `.env`（Git管理外）と GitHub Secrets に置く。チャットに貼らない。workflowへ渡すのは `RESEND_API_KEY` と `REPORT_TO` だけ。`ANTHROPIC_API_KEY` はClaude経路の再有効化・堅牢化まで渡さない。
- 本番実行は GitHub Actions（`.github/workflows/weekly-news-report.yml`）。依存は `requirements.lock` のバージョン・ハッシュ、外部ActionはコミットSHAで固定。手動実行は workflow_dispatch ボタン。
- 金利履歴の入力元は非公開 `yoshiyukimano-hub/financial-report-tool` の週次生成直後の `report_data_*.json`。同リポジトリから専用Deploy Keyで本リポジトリの履歴JSONだけを更新する。
- **リポジトリは public**。秘密情報・個人情報をコミットに含めない。送信先メールは `REPORT_TO` Secret（コード・ドキュメントに実値を書かない）。
- コミット用の `user.email` はこのリポジトリで noreply(`270032650+yoshiyukimano-hub@users.noreply.github.com`)に設定済み。Gmail等の個人メールで再コミットしないこと。別環境で作業する場合も同様に設定する。
- **履歴は書き換え済み（2026-07-19）。旧clone・旧ブランチを復活させない**。別の場所にclone があれば作り直す（`git reset --hard origin/main` かクローンし直し）。
- push 先は **origin/main** のみ。
- pre-commit フックを**スキップしない**（`--no-verify` 禁止）。クローン直後に1回 `git config core.hooksPath .githooks` で有効化する。
- 参照専用プロジェクト `C:\Users\mano\src\AI-trend-weather-News` は**変更禁止**（Read のみ）。
